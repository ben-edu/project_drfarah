# API — Dr. Farah VIP Urgent Care

Booking and administration API served at `api.drfarah.proxbenovh.cloud` and
`api.staging.drfarah.proxbenovh.cloud`.

## Future responsibility

- FastAPI, versioned routes under `/api/v1`.
- Booking management, schedule calculation, contact handling.
- Containerized, deployed to K3s / BM2 via Jenkins.
- PostgreSQL database for persistence.

## Status

Implementation has not started. See `docs/READINESS_AUDIT.md` for current
readiness and `PROJECT.md` for the approved product brief.
