# Kubernetes — drfarah namespace

Resources for the `drfarah` namespace on the `afpa-k3s` cluster (BM2).

## Future responsibility

- Namespace, ConfigMap, Secret, PostgreSQL StatefulSet + Service + PVC,
  Deployment, ClusterIP Service, Traefik Ingress.
- Harbor pull secret (copy from reference pattern).
- Commit only `*.example` files for secrets.

## Status

Implementation has not started. See `docs/READINESS_AUDIT.md` for current
readiness and `PROJECT.md` for the approved product brief.
