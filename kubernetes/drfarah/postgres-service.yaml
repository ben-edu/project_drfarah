apiVersion: v1
kind: Service
metadata:
  name: drfarah-postgres
  namespace: drfarah
  labels:
    app: drfarah-postgres
spec:
  type: ClusterIP
  selector:
    app: drfarah-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
